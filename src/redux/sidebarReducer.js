let initilState = {
    sidebar: [
        {id: 1, name: 'Oly'},
        {id: 2, name: 'Mas'},
        {id: 3, name: 'Vas'},
    ]
}

const sidebarReducer = (state = initilState, action) => {

    return state;
}
export default sidebarReducer;